# COPY — the writing library

Every line of copy written for this site that is worth keeping lands here, whether or not it is
currently in use. Nothing is deleted when it loses a round. A line rejected for the hero is often
the right line for a section heading three screens down, and a line that reads wrong in August
may read right in October.

Brad chooses. The job of this folder is to make sure the choice is always available.

## Structure

```
COPY/
├── README.md              this file
├── blog/                  blog posts, plus Brad's two fiction-register style examples
│   ├── cubs-uniform.md
│   └── plastic-bags.md
├── career/                the career record — role sections, career rows
│   └── pre-education-career.md
├── hero-lines/            display lines for the top of a page
│   └── hero-lines.md
├── letters/               the /letters page — headline, captions, pull-quotes, blocks
│   ├── letters-page.md
│   └── quote-bank.md      Brad's own selection of reference excerpts for evidence slots
├── navigation/            nav labels, page headlines, glosses, the CV button
│   └── navigation.md
├── positioning/           raw positioning-line brainstorm, not scored against the claims register
│   └── positioning-lines.md
└── section-headings/      headings, eyebrows, captions and calls to action
    └── section-headings.md
```

Folders are added as new categories of copy appear. `letters/` replaces the `proof/` folder predicted
here, along with the route. `career/` is the `record/` folder that was predicted here, added 14
August 2026 with the first Phase 3 draft. `blog/` was added 15 August 2026 with the first two
row-four drafts — it replaces the `field-notes/` folder predicted here. The 15 August "Brad calls it
the blog" note is superseded: the reader-facing name, chosen 17 August 2026 and implemented the same
day, is **Field notes & stories** (nav label, `/blog/` eyebrow pill and page title) — the internal
route and folder stay `/blog/` and `COPY/blog/` per `COPY/blog/Naming the blog section.md` §1.1. It
holds his two fiction-register style examples, which were misfiled in `career/`. **The
blog is not written in the §7 voice**; its own spec is `design/09-blog-voice.md`, it has its own
README, and its entries are stored against that spec instead. `positioning/` was added 17 August 2026
to give the loose root-level brainstorm file (`resume-website-positioning-lines.md`) a home, a
status, and a re-cut against the sentence-opener rule below.
README, and its entries are stored against that spec instead. Still likely: `cv/` for the four
variant summaries.

`interviews/` sits alongside these and is not copy — it is the raw record and the claims register
that Phase 3 drafts from. Nothing in it is approved for the site.

## Status key

Every entry carries one of four statuses.

| Status | Meaning |
| --- | --- |
| **In use** | Currently placed on the site. The file says where. |
| **Available** | Written, holds up, not currently placed. Ready to swap in. |
| **Held** | Deliberately reserved for a specific slot elsewhere on the site. |
| **Rejected** | Turned down by Brad, kept for the record so the same ground is not covered twice. |

## Rules for entries

- Every option carries the argument for it **and** the argument against it. An option with no
  stated cost has not been thought about properly.
- Support copy is stored with its display line, because the two are judged together.
- New Zealand English. The banned-words list is section 7 of `design/01-positioning-brief.md`.
- Date every status change. Copy decisions get revisited and the reasoning matters more than
  the verdict.
