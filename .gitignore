node_modules/
dist/
.astro/
.DS_Store
.venv/
.review-shots/
.shots/
New approach images/

# Source interviews and the claims register — local only, never committed.
# Raw evidence, contested figures and gag-covered context. The site never reads these.
COPY/interviews/

# Local Netlify folder
.netlify

# Raw design sources for the client logo archive — .ai/.psd/.doc binaries git cannot
# diff and that bloat the clone. Kept locally; the site ships only exported derivatives.
Brad Friis Resumes/Company logos - who Ive worked with/99 Corporation/
