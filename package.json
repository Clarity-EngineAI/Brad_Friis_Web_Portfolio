{
  "name": "brad-friis-personal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check:fit && astro check && astro build",
    "check": "astro check",
    "preview": "astro preview",
    "check:fit": "node scripts/check-fit-model.mjs && node scripts/check-fit-claims.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@sanity/client": "^8.1.0",
    "astro": "^7.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
