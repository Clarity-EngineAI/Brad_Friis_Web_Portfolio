# Next session

Model: Opus (multi-file content work)

## Status

GCT-naming conflict is resolved (29 Aug 2026, commit 386364f). All 5 CV variant files
(`Brad Friis Resumes/bradfriisresume*20260806.md` + their `.docx` originals in
`IGNORE -DOCX VERSIONS RESUMES/`) had "GCT Education" replaced with the approved
"PowerSchool channel partner · education sector" framing (matches `cv.astro`). Also fixed
during the sweep: a live `brad@gct.education` address in the Rototuna reference letter
transcript (redacted), and a superseded LinkedIn profile export that named GCT directly
(deleted; only `Brad Friis LinkedIn Revised.pdf` remains as source of truth).

The 99 Corporation Reference letter (Rob Nieuwland) is the approved replacement for the
old 2026 resume files, which have been deleted and committed.

## Next task: 5th CV variant + reconciliation

Brad confirmed (29 Aug 2026) he still wants the missing 5th CV variant built —
website-development or brand-identity/communications, matching the style/format of the
existing 4 (`bradfriisresumeb2bsaasaccountmanager`, `channelpartnermanager`,
`ecommerceretailtechcustomersuccess`, `professionalservicessoftwareaccountmanager`, plus
`master`). Confirm which of the two (website-dev vs brand-identity/communications) with
Brad before drafting — this wasn't disambiguated.

Once that exists, reconcile all 5 variants against `COPY/interviews/_claims-register.md`
and decide the delivery mechanism (separate `/cv/*` pages vs role-switcher vs static PDFs)
before touching `cv.astro`.

## Also open, untracked

Client Questionnaire thread (`Client Questionnaire/` folder) — Brad mentioned this exists
but scope was never briefed: which client, what was asked, what's been submitted, deadline,
whether any site content needs to feed into it. Get a briefing before doing anything with it.

Next: start a new session (Opus) to confirm the 5th CV variant's exact positioning with Brad, draft it, then reconcile all 5 against the claims register.
